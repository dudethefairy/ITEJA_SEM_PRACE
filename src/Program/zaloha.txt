const g:int=10;
var x:string, z:double, d:double;

procedure proc();
const i:int=5;

PROCEDURE proc2();
CONST p:int=5;
VAR y:int;
BEGIN
!"Zadejte y: ";
?y;
d :=y*p
END;

BEGIN
CALL proc2();
!"Zadejte x: (x je string a je převeden na číslo pomocí +)";
?x;
z :=((+x)*(i+(d/(g))))
END;

PROCEDURE kdyz(pepa:string,jana:int);
BEGIN
!pepa;
!jana;
IF z<100 THEN !"Hurá";
END;

program bankovniucet;
var pocet:int,jmeno:string, prijmeni:string, vek:int, penize:double, prijem:double,prijmy:double, vybrani:double, i:int;

procedure nastavOsobu(name:string,surname:string,age:int,money:double,income:double);
var novyVek:int;
begin
set novyVek:=20;
set jmeno:=name;
set prijmeni:=surname;
set vek:=novyVek;
set penize:=money;
set prijem:=income;
if pocet=0 then begin
set pocet:=1;
pcall nastavOsobu('Petra','Janeckova',25,4500,3000)
end;
end;

function zjistiPrijmy(pocetMesicu:int,mesicniPrijem:double):double;
begin
zjistiPrijmy:=pocetMesicu*mesicniPrijem
end;

begin
set pocet:=0;
pcall nastavOsobu('Petr','Novak',35,5000,2000);
set prijmy:=fcall zjistiPrijmy(fcall zjistiPrijmy(fcall zjistiPrijmy(vek,2),2),prijem);

writeln(jmeno+' '+prijmeni+' Příjmy:');
writeln(prijmy);
writeln('Zustatek');
writeln(penize)

writeln('Zadejte castku k vybrani:');
readln(vybrani);

repeat begin
set penize:=penize-vybrani;
writeln('Vybrano:');
writeln(vybrani);
writeln('Zustatek');
writeln(penize);
end
until penize>2000

for i:=10 to 0 do begin
writeln(i*penize)
end;
end. 

PROCEDURE while();
BEGIN
WHILE z>0 DO BEGIN
z:=z-1;
IF ODD z THEN !z;
END
END;

BEGIN
!"Příklad";
!"z=(x)*(5+(d/(10)))";
!"d=y*5";
CALL proc();
!"Výsledek: ";
!z;
!"Jestli je z<100 vypiš Hurá!";
CALL kdyz("lama",10);
!"Dokud je z>0 odečti od něj jedna a vypiš každou lichou hodnotu z!";
CALL while()
END.

